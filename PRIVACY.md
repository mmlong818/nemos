# 小丑鱼隐私协议

版本：0.2.3
生效日期：2026 年 8 月 17 日

本协议说明小丑鱼如何处理数据。小丑鱼是本机优先的 AI 工作应用；项目当前不提供托管账号、广告、行为画像或集中式遥测服务。

## 1. 默认保存在本机的数据

小丑鱼默认把下列内容保存在当前 Windows 用户的本机数据目录中：

- 任务、对话、学习记录和用户主动保存的记忆；
- 导入的文件、转换副本、导出文件、能力产物和开发记录；
- 模型、开发引擎、连接器、外观和数据保存设置；
- 权限确认、工具调用、错误、恢复点和必要的审计记录。

默认目录是 `~/.clownfish`。用户可以在“设置 → 数据与隐私”查看实际位置。除非用户启用外部服务或同步，小丑鱼不会把这些数据上传到由本项目运营的服务器。

## 2. 何时数据会离开本机

只有在用户配置、启用或明确调用相应功能时，数据才会发送给第三方或用户自己的服务：

- **模型服务**：当前请求、必要的上下文、用户选择的附件内容以及工具结果可能发送给所选模型服务商；
- **联网搜索与网页读取**：查询词和目标网址会发送给相应搜索或网站服务；
- **插件和连接器**：只获得安装前展示并经用户确认的权限与输入；
- **开发引擎**：用户选择的项目上下文会交给所选开发引擎及其配置的模型服务；
- **自托管同步**：用户启用后，加密快照会发送到用户指定的服务器。

第三方服务如何保存和使用数据，由其自身协议决定。使用外部服务前，请不要提交不应交给该服务处理的个人信息、商业秘密或受限制资料。

## 3. 密钥与凭证

在支持的 Windows 环境中，模型密钥和同步凭证使用当前用户的系统加密能力保存在本机。小丑鱼不会把密钥写入项目文件、能力产物或同步快照。第三方命令行工具可能使用它们自己的凭证目录和隐私规则。

## 4. 记忆系统

记忆用于保持用户明确表达的事实、偏好和任务连续性。用户事实、角色内容和不同对话拥有独立边界；测试材料、附件文本和模型推断不会自动被当成永久用户事实。用户可以查看、修正、失效或删除整理后的记忆。

## 5. 文件与开发项目

- Office、PDF 等文件默认保留原件，并以转换副本继续编辑；
- TXT 和 Markdown 只有在用户明确授权且冲突检查通过后才会写回原文件；
- 开发能力只处理用户明确选择或关联的目录；修改权限、依赖安装、提案应用和删除目录分别受确认与边界检查；
- 删除界面记录时，用户可以选择是否同时删除受管文件。外部项目目录不会因删除界面记录而自动删除。

## 6. 插件、权限与自动化

插件安装前会显示来源、权限、依赖和是否启动本机程序。权限扩大或运行结构变化需要重新确认。自动化只执行用户创建或启用的计划；产生外部副作用的操作仍受相应权限规则约束。

## 7. 自托管同步与备份

纯本地模式不会使用同步服务器。启用自托管同步时，快照在本机加密后上传；模型密钥和本机凭证不进入快照。用户需要自行保护同步令牌和加密口令；加密口令丢失后，服务器快照可能无法恢复。

## 8. 数据保留、导出和删除

数据在用户设备上保留，直到用户删除、归档、恢复备份或卸载时主动处理。用户可以通过应用导出文件、清理记忆、删除任务与产物。删除前请确认是否需要保留导出文件或备份；被彻底删除且没有备份的数据无法恢复。

卸载应用不应被理解为必然删除 `~/.clownfish`、外部项目目录或用户导出的文件。

## 9. 未成年人

学习辅导功能可以服务未成年人，但应用不会据此创建广告画像。监护人应根据学习材料、外部模型服务及所在地规则决定是否允许使用，并避免提交不必要的身份和联系方式。

## 10. 安全边界

本机网页服务仅设计为监听回环地址，不应直接暴露到公网。任何软件都不能保证绝对安全；发现安全问题时，请按 [SECURITY.md](SECURITY.md) 使用 GitHub 私密漏洞报告入口，不要在公开 Issue 中附上个人数据、密钥或可直接利用的细节。

## 11. 协议更新

隐私处理范围发生实质变化时，本协议会提升版本并在发布说明中列明。涉及新增数据外发、权限扩大或默认保存方式变化的更新，应在启用相关功能前向用户明确展示。

## 12. 联系方式

- 一般隐私问题：通过 GitHub Issue 提交，但不要附带私人数据；
- 安全或数据泄露问题：使用 GitHub 仓库的私密漏洞报告入口，具体方式见 [SECURITY.md](SECURITY.md)。
