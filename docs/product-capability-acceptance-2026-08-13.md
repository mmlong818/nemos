# 产品能力验收记录（2026-08-13）

本文档记录本轮能力提升的实际交付边界与验证证据。状态只使用“实验、可用、已验证、生产就绪”四档；没有证据的能力不升级状态。

## 验收结论

| 能力 | 当前状态 | 本轮证据 | 明确边界 |
| --- | --- | --- | --- |
| Office 结构化编辑副本 | 已验证 | 20 份脱敏 DOCX 往返转换全部通过；Microsoft Word COM 打开 20/20；真实 DOCX、PDF、PPTX、XLSX 生成与重新解析通过 | 原文件只读；复杂浮动对象、批注、跨节页眉页脚、公式、图表、母版仍使用原文件或桌面 Office |
| 开发项目工作台 | 已验证 | 10 类小项目完成定位、修改、检查、逐文件应用与回滚；越界路径和敏感文件被拒绝 | 命令来自固定白名单；项目修改仍需用户确认 |
| 统一交付物 | 已验证 | 聊天、能力、任务和文件使用稳定产物编号、版本、来源与工作区关系 | 不把不同结果复制成互不关联的文稿 |
| 技能生命周期 | 已验证 | 安装、升级、回滚、停用、启用、卸载在真实服务与集成测试中通过 | 可执行扩展仍需权限预览和显式确认 |
| 六类连接器 | 可用 | 本地文件、浏览器内置连接测试通过；GitHub、邮箱、日历、企业文档准确显示为未安装扩展 | 未安装的连接器不宣传为可用，不模拟账号连接 |
| 五类领域能力包 | 可用 | 研究、办公、开发、运营、财务共 10 个固定正反样例通过 | 固定样例只证明边界与基本质量，不等同于所有场景已核验 |
| 记忆驱动习惯 | 已验证 | 单次任务可选择使用偏好或完全关闭；结果明确显示本次采用的偏好 | 不把所有历史内容默认注入每个任务 |
| 后台、并行与审阅 | 已验证 | 持久任务队列、并行编排、恢复检查点、交付确认和统一审阅队列均有回归测试 | 有副作用的中断任务不会自动重放 |
| 质量状态体系 | 已验证 | 产物必须经过结构、领域或真实工具检查才能升级状态 | 文件存在或人工点击不能直接升级为“生产就绪” |

## 实际运行证据

- `npm.cmd run check`：构建、类型检查和 369 项自动化测试全部通过。
- `npm.cmd run quality:office-corpus`：20/20 份 DOCX 结构化往返通过。
- `npm.cmd run quality:office-word`：Microsoft Word 打开 20/20。
- `npm.cmd run quality:development-corpus`：10/10 类小项目闭环通过。
- `npm audit --offline --audit-level=high`：未发现已知高危依赖问题；本轮未向外部审计服务发送依赖清单。
- 本轮新增 10 轮真实使用检查；应用“工作 → 运行”页面累计保存 30 轮记录，当前无未修问题。
- Docker 同步服务完成健康检查、错误令牌拒绝、端到端加密上传、下载和重启恢复；受控依赖安装完成真实 npm 锁定安装。

## 产品级原则

1. 原始文件、用户数据和项目目录优先可恢复，不用宣传口径替代真实回执。
2. 能力入口面向任务，不要求普通用户先理解模型、角色或命令行。
3. 内置能力、运行基础设施和需要外部扩展的连接器分别标注。
4. 失败必须留下原因、检查点和可继续入口；不能把“生成了内容”当作“交付已通过”。
