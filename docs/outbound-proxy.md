# 出站代理

模型服务商的 `fetch` 请求可以使用显式配置或环境提供的 HTTP(S) 代理。代理默认关闭；`off` 模式不会读取代理环境变量，也不会改变全局 dispatcher。

显式代理地址不得内嵌用户名或密码。需要认证时应通过运行环境提供凭据，应用不会把代理密码写入普通配置文件。回环地址、私网地址和当前模型端点的本地主机始终绕过代理，避免本机模型流量被送往外部代理。

代理只覆盖声明支持的 `fetch` 出站路径。网页读取使用独立的 DNS 钉住与重定向校验，不会因模型代理配置而绕过内网保护。界面与日志必须清楚区分代理未配置、代理连接失败和模型服务拒绝。
