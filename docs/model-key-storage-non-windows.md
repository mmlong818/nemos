# 非 Windows 模型密钥存储限制

小丑鱼当前使用 Windows 当前用户的 DPAPI 加密并保存模型密钥与连接器凭据。因此：

- Windows 可以保存并在后续启动中恢复凭据；
- Linux 和 macOS 可以运行网页服务，但当前不能通过应用持久化模型密钥；
- 非 Windows 平台不会退回明文文件或较弱的自制加密方案；
- 没有成功保存模型连接时，依赖模型的任务不会运行，本机数据浏览仍可使用。

跨平台凭据存储只有在接入各平台成熟的系统密钥库、定义迁移与删除语义并完成真实环境测试后才会开放。
