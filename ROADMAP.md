# 路线图

更新：2026-08-10

路线图分为记忆 SDK 与小丑鱼应用两条线。版本和功能以代码、测试与发布记录为准。

## 当前版本

| 组件 | 版本 | 状态 |
|---|---|---|
| Nemos Memory SDK | 0.7.5-alpha.17 | TypeScript 嵌入式 Alpha |
| 小丑鱼 | 0.2.19 | 本机应用 Alpha |

## 已完成：记忆 SDK

- SQLite 与内存存储；
- 原始归档和四类派生记忆；
- 场景化抽取与长文本处理；
- 同步和后台写入共享生命周期；
- 结构化 claim、纠正、失效、争议和身份操作；
- 关键词、实体、时间、向量和证据召回；
- MemoryPacket、可靠性、预算和召回解释；
- salience 与 evidence coverage；
- 反思、衰减、领域路由和前瞻记忆模块；
- JSON-LD 与 Markdown 导出；
- 用户与 tenant 隔离。

## 已完成：小丑鱼

- 聊天、能力、文件和工作四个主入口；
- 通用模型连接与离线模式；
- 13 项公开能力；
- 持久任务、运行、审批和交付物；
- 取消、重试、恢复、计划任务和实时进度；
- Skill、MCP、Agent App 注册和权限边界；
- Node 权限沙箱与 Windows LPAC 宿主；
- 受控多 Agent 编排；
- Office 读取、原版式预览、工作副本、版本、页内处理和导出；
- 文件按格式声明真实能力（可编辑／可批注／仅查看／需转换），界面不按扩展名猜测；
- 记忆查看、明确添加习惯和忘记分类项；
- Windows 便携客户端。
- 首页默认只保留小丑鱼，陪伴与学习角色改为按需添加；
- 对话、能力结果可直接进入文件工作台继续编辑；
- 能力任务记录本次实际采用的交付习惯。
- 对话、能力接力和直接执行统一沉淀为可恢复任务；
- 同一成果继续处理时沿用任务并记录版本关系；
- 一次任务可直接转为每日或按使用频率重复执行。

## 近期重点

### 记忆

- 支持单次忽略偏好；
- 加强多用户服务部署样例与隔离测试；
- 继续校准长期召回、时间解释和拒答；
- 明确 archival 删除与数据主体请求流程。

### 小丑鱼

- 提升 DOCX、PPTX 和 XLSX 原版预览能力；
- 用更多真实 Office 文件验证同一成果跨页面的版本关系和来源定位；
- 完善能力质量评估和人工审查；
- 扩大浏览器与便携客户端真实使用测试；
- 继续降低新用户配置成本。

### 文件能力建设顺序

文件能力按格式分批交付。每一批只有通过真实文件回归和外部软件打开验证后，
才把该格式的能力标注从"仅查看"提升为"可编辑"。

| 阶段 | 范围 | 状态 |
| --- | --- | --- |
| P0 | 按格式声明真实能力，停止一切"无损编辑 Office"的表述 | 已完成 |
| P1 | 统一文件会话与编辑器接口，先交付 TXT/Markdown 的完整编辑 | 进行中 |
| P2 | DOCX：段落级保真文字修改已完成并通过 Word 实机验证；分页视图、样式与批注修订编辑未开始 | 部分完成 |
| P3 | XLSX/CSV/TSV：多工作表、公式、格式、图表 | 未开始 |
| P4 | PPTX：母版、版式目录、画布编辑与视觉质量门 | 未开始 |
| P5 | PDF：批注、表单、页面操作，之后才是内容编辑 | 未开始 |
| P6 | 可选的浏览器端 Office 服务与多人协作 | 未开始 |

当前实现的边界：

- TXT 和 Markdown 可以直接编辑并写回原文件；
- DOCX 可以按段落修改文字，未改动内容与改动段落自身的行内格式都保持原样，
  既能写回原文件也能另存为新文件（写回前的版本保留在版本记录里），
  不能增删段落、改样式、改页眉页脚批注修订；
- PPTX 和 XLSX 仍走旧的文字级替换，会合并被改动位置的行内格式，因此标为仅查看；
- PDF 只查看。

"可编辑"与"能否覆盖原文件"在能力表里是两个独立字段，不互相冒充。
把某个格式提升为"可编辑"之前必须有实机保真证据，见
`sdk/typescript/examples/companion/docs/reviews/`。这些限制在文件页逐条显示，
不放在文档里含糊带过。Office 兼容性只声明 Microsoft Word，不声称兼容 WPS 与 LibreOffice。

## 质量标准

1. 构建通过；
2. 全部测试通过；
3. 文档链接有效；
4. README 截图来自当前界面和干净示例数据；
5. 无真实密钥、用户数据库、日志或私人配置；
6. 对外能力与当前接口一致；
7. 发布版本与变更记录一致。
